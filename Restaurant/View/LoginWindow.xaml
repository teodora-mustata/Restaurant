<Window x:Class="Restaurant.View.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Restaurant.View"
        mc:Ignorable="d"
        Title="LoginWindow" Height="300" Width="300">
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Images/login_background.jpg" Stretch="UniformToFill"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Login" 
   FontSize="36" 
   FontWeight="Bold" 
   Foreground="White" 
   HorizontalAlignment="Center" 
   VerticalAlignment="Top" 
   Margin="0,10,0,0" 
   FontFamily="Segoe Script"
                   Grid.Row="0"/>
        <Label Content="Email" FontWeight="Bold"  Foreground="White" Grid.Row="1"/>
        <TextBox x:Name="EmailTextBox" Text="{Binding Email}" Margin="0,5" Grid.Row="2"/>

        <Label Content="Password" FontWeight="Bold"  Grid.Row="3" Foreground="White"/>
        <TextBox x:Name="PasswordBox" Text="{Binding Password}" Margin="0,5" Grid.Row="4"/>

        <Button Content="Login" FontWeight="Bold"  Grid.Row="5" Margin="0,10" Height="40" Command="{Binding LoginCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Background="White"/>
    </Grid>
</Window>
