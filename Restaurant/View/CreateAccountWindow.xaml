<Window x:Class="Restaurant.View.CreateAccountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Restaurant.View"
        mc:Ignorable="d"
        Title="CreateAccountWindow" Height="500" Width="350">
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Images/create_account_background.jpeg" Stretch="UniformToFill"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Create Account" 
FontSize="36" 
FontWeight="Bold" 
Foreground="White" 
HorizontalAlignment="Center" 
VerticalAlignment="Top" 
Margin="0,10,0,0" 
FontFamily="Segoe Script"
                Grid.Row="0"/>

        <Label Content="First Name" FontWeight="Bold"  Grid.Row="1" Foreground="White"/>
        <TextBox x:Name="FirstNameTextBox" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Grid.Row="2"/>

        <Label Content="Last Name" FontWeight="Bold"  Grid.Row="3" Foreground="White"/>
        <TextBox x:Name="LastNameTextBox" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Grid.Row="4"/>

        <Label Content="Email" FontWeight="Bold" Grid.Row="5" Foreground="White"/>
        <TextBox x:Name="EmailTextBox" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Grid.Row="6"/>

        <Label Content="Password" FontWeight="Bold"  Grid.Row="7" Foreground="White"/>
        <TextBox x:Name="PasswordBox" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Grid.Row="8"/>

        <Label Content="Phone Number" FontWeight="Bold"  Grid.Row="9" Foreground="White"/>
        <TextBox x:Name="PhoneTextBox" Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Grid.Row="10"  
                 PreviewTextInput="PhoneTextBox_PreviewTextInput"  MaxLength="10"/>

        <Label Content="Address" FontWeight="Bold"  Grid.Row="11" Foreground="White"/>
        <TextBox x:Name="AddressTextBox" Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Grid.Row="12"/>

        <Button Content="Create Account" FontWeight="Bold" Grid.Row="13" Height="40" Margin="0,10" Command="{Binding CreateAccountCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Background="White"/>
    </Grid>
</Window>
