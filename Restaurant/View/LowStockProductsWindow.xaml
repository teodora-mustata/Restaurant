<Window x:Class="Restaurant.View.LowStockProductsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Restaurant.View"
        mc:Ignorable="d"
        Title="LowStockProductsWindow" Height="800" Width="800">
    <Grid>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

        <ListBox ItemsSource="{Binding LowStockProducts}"
         Grid.Row="0" Margin="0,0,0,10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Image Source="{Binding ImagePath}" Width="100" Height="100" Margin="5"/>
                        <StackPanel Margin="5">
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="Red"  />
                            <TextBlock Text="{Binding Category.Name, StringFormat='Category: {0}'}"  />
                            <TextBlock Text="{Binding QuantityPerServing, StringFormat='Quantity per serving: {0}g'}"/>
                            <TextBlock Text="{Binding TotalQuantity, StringFormat='Total quantity: {0}g'}"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
               Text="List of Low Stock Products"
               FontWeight="Bold"
               VerticalAlignment="Center" 
               Foreground="Red"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Low Stock Multiplier:" Width="120" HorizontalAlignment="Right"/>
                    <TextBlock Text="{Binding LowStockMultiplierDisplay}" Width="40" HorizontalAlignment="Right"/>
                </StackPanel>
            </Grid>


        </Grid>
    </Grid>
</Window>
