<Window x:Class="Restaurant.View.EditCategoriesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Restaurant.View"
        mc:Ignorable="d"
        Title="EditCategoriesWindow" Height="450" Width="800">
    <Grid>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ListBox ItemsSource="{Binding Categories}"
                 SelectedItem="{Binding SelectedCategory, Mode=TwoWay}"
                 DisplayMemberPath="Name"
                 Grid.Row="0" Margin="0,0,0,10" />

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBox Text="{Binding CategoryName, Mode=TwoWay}" Width="180" Margin="0,0,10,0"/>
                <Button Content="Save changes" Width="80" Command="{Binding UpdateCategoryCommand}" />
                <Button Content="Delete" Width="80" Margin="10,0,0,0" Command="{Binding DeleteCategoryCommand}" />
            </StackPanel>

            <Button Content="Add New Category"
                Grid.Row="2" Height="40" Background="LightGreen"
                Command="{Binding AddCategoryCommand}" />
        </Grid>
    </Grid>
</Window>
